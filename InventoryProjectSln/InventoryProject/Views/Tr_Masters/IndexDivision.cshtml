@model InventoryProject.Models.DivisionAndDistrictVM
@{
    ViewBag.Title = "IndexDivision";
}

<h2>IndexDivision</h2>

<div class="panel panel-body" style="min-height:256px">
    <div class="col-md-3">
       
    </div>
    <div>
        @if (ViewBag.DivisionList !=null)
        {
            @Html.DropDownListFor(m => m.DivisionID, ViewBag.DivisionList as SelectList, "..Select..", new { @class="form-control"})
        }
        
        @Html.DropDownListFor(m => m.DistrictID, new SelectList(""), "..Select..", new { @class = "form-control" })
    </div>
</div>
 
@*<script>
    $(document).ready(function () {
        $("DivisionID").change(function () {
            var divisionId = $(this).val();
            $.ajax({
                type: "POST",
                url: "Tr_Masters/GetDistrictlist",
                data: FormData,
                contentType: false,
                processData: false,
                success: function()


            })

        })
    })
</script>*@