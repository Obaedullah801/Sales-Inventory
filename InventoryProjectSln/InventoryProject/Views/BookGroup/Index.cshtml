@model IEnumerable<InventoryProject.Models.BookGroup>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MainBookName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Class)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MainBookName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Class)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BookGroupID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BookGroupID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BookGroupID })
        </td>
    </tr>
}

</table>
